# SsangBak project instructions

이 저장소는 `https://ssangbak.com`을 운영하는 Astro 정적 사이트다. 모든 작업에서 아래 규칙을 적용한다.

## 작업 시작 전

- 기능·구조 변경 전에는 `docs/PROJECT_SPEC.md`와 `docs/CURRENT_STATUS.md`를 읽는다.
- 콘텐츠 작성·수정 전에는 `docs/SSANGBAK_CONTENT_STANDARD.md`를 읽는다.
- 다음 작업의 우선순위는 `docs/ROADMAP.md`를 기준으로 판단한다.
- 관련 문서와 실제 코드가 다르면 코드를 확인하고, 차이를 `docs/CURRENT_STATUS.md`에 반영한다.

## 콘텐츠 원칙

- 독자가 실제로 판단하고 행동할 수 있는 고유 정보를 제공한다. 검색어만 바꾼 얕은 글을 만들지 않는다.
- 일정·금액·대상·가격·법령은 작성 당일의 공식 1차 출처로 확인한다.
- 출처가 확인되지 않은 사실, 경험, 처리시간, 혜택을 만들어내지 않는다.
- 종료된 공연·신청·이벤트를 신규 글감으로 선정하지 않는다. 종료 후에도 검색 가치가 있는 후속 절차는 예외다.
- 같은 검색 의도와 결론을 가진 기존 글이 있으면 중복 발행하지 않고 기존 글을 보강한다.
- 시리즈는 같은 카테고리가 아니라 독자의 다음 행동이 이어지는 글끼리 연결한다.
- 신청·예약·조회·다운로드 키워드는 사용할 수 있다. 공식 기관·제조사로 오인시키거나 광고 클릭을 유도하는 구조는 금지한다.
- CTA는 약속한 목적지로 직접 연결하고, 광고와 공식·제휴 링크를 명확히 구분한다.
- 썸네일은 기존 편집형 품질과 모바일 가독성을 유지한다. 대량 작업에서도 임시 저품질 이미지를 발행하지 않는다.

## 광고·수익화 원칙

- 사용자 승인 없이 유료 광고 캠페인을 활성화하거나 예산을 집행하지 않는다.
- 심사 회피, 브리지 페이지, 반복 전면광고, 공식 다운로드·신청 페이지 사칭, 계정 제재 우회 구조를 구현하지 않는다.
- 유료 광고는 전환 추적과 랜딩페이지 검증을 마친 뒤 소액 검색 캠페인부터 시작한다.
- 제휴 링크가 있는 글은 필요한 고지 문구를 명확히 표시한다.

## 코드·배포 원칙

- 기존 사용자 변경과 무관한 파일을 삭제하거나 되돌리지 않는다.
- 콘텐츠만 변경해도 최소 `npm run content:check`를 실행한다.
- 레이아웃·컴포넌트·설정·다수 콘텐츠를 변경하면 `npm run build:quiet` 또는 `npm run build`를 실행한다.
- 배포 작업은 빌드 통과 후 진행하고, 실제 공개 URL·이미지·사이트맵을 표본 확인한다.
- 사이트맵 정식 주소는 `/sitemap-index.xml`이다. `/sitemap.xml`과 `/sitemap_index.xml`은 여기로 리디렉션된다.
- 비밀키와 인증정보는 저장소 문서, 소스, 커밋에 기록하지 않는다.

## 작업 완료 시

- 기능·운영 상태·우선순위가 바뀌면 `docs/CURRENT_STATUS.md`를 갱신한다.
- 향후 계획이 바뀌면 `docs/ROADMAP.md`를 갱신한다.
- 재현 가치가 있는 결정이나 운영 원칙 변경은 `docs/CHANGELOG.md`에 이유와 함께 기록한다.
- 단순한 개별 글 발행은 `CHANGELOG.md`에 일일이 기록하지 않고 Git 이력으로 관리한다.
